__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Deliberately unpinned: this project tracks the latest duckdb and mcp releases
# rather than a frozen resolution. See "Development" in the README.
uv.lock
